// reduce()
// array.reduce() leva dois parâmetros. O primeiro é o acumulador
// e o segundo é o valor inicial que podemos definir.

// Syntax:
// reduce((accumulator, currentValue, currentIndex, array))

// Example:
const arr = [1, 2, 3, 4, 5];

let result = array.reduce(function (accumulator, currentValue) {
  return acc + val;
}, 0);

console.log(result);

// Output:
// 15
